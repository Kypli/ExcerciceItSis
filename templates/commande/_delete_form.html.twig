<form method="post" action="{{ path('commande_delete', {'id': id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette commande ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
    <button class="btn">Supprimer la commande</button>
</form>
